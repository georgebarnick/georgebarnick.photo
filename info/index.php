<!doctype html>
<html>
<head>
<meta charset="UTF-8"/>
<meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<meta name="description" content="George Barnick Automotive Photography - Professional automotive photography based in Northern Virginia"/>
<meta name="keywords" content="photography, cars, exotic cars, automotive photography, photographer"/>
<meta property="og:title" content="George Barnick Automotive Photography" />
<meta property="og:type" content="business.business" />
<meta property="og:url" content="https://www.georgebarnick.photo/" />
<meta property="og:image" content="https://www.georgebarnick.photo/images/lotus.jpg" />
<meta property="og:description" content="Professional automotive photography based in the Northern Virginia and Washington DC area" />
<meta property="business:contact_data:street_address" content="521 Winterberry Drive" />
<meta property="business:contact_data:locality" content="Fredericksburg, VA" />
<meta property="business:contact_data:postal_code" content="22405" />
<meta property="business:contact_data:country_name" content="United States" />
<meta property="business:contact_data:website" content="https://www.georgebarnick.photo" />
<meta name="theme-color" content="#001722">
<link rel="icon" type="image/x-icon" href="../images/favicon.ico" />
<link rel="icon" sizes="192x192" href="../images/favicon.png">
<title>George Barnick Photography</title>
<style type="text/css">
* {
        margin: 0;
        padding: 0;
}

body {
        background-color: #001722;
}

#logo {
        background-image: url( ../images/logo.png );
        background-size: contain;
        background-repeat: no-repeat;
        background-position: top center;
        width: 100%;
        height: 65px;
        margin-top: 36px;
}

#avatar {
        width: 600px;
        max-width: 900px;
        margin-left: auto;
        margin-right: auto;
        margin-top: 16px;
}

#avatar img {
        width: 100%;
        height: auto;
        box-shadow: 0 0 1em #000;
}

.nav {
        list-style-image: none;
        font-family: "fira-sans", sans-serif;
        text-align: center;
        margin-top: 16px;
        margin-left: 20%;
        margin-right: 20%;
        border-bottom: 3px solid #3F5058;
        padding-bottom: 28px;
}

.nav a {
        font-size: 18px;
        text-decoration: none;
        color: #fff;
        margin-left: 12px;
        margin-right: 12px;
        transition: all .25s ease-in-out;
}

.nav a.selected,
.nav a:hover,
.clients a:hover {
        color: #FFCF0B;
}

.clients {
        font-family: "fira-sans", sans-serif;
        text-align: center;
        margin-top: 16px;
        margin-left: 20%;
        margin-right: 20%;
        border-bottom: 2px solid #3F5058;
        padding-bottom: 1em;
        color: #546b76;
        display: table;
}

.clients a {
        font-size: 16px;
        text-decoration: none;
        color: #546b76;
        margin-left: .5em;
        margin-right: .5em;
        transition: all .25s ease-in-out;
}

.clients-cell {
        display: table-cell;
        vertical-align: top;
}

h1 {
        text-align: center;
        color: #fff;
        font-family: "fira-sans", sans-serif;
        margin-top: 36px;
}

p {
        color: #cecece;
        font-family: "fira-sans", sans-serif;
        font-size: 18px;
        margin-left: 20%;
        margin-right: 20%;
        margin-top: 20px;
        width: 60%;
        line-height: 2em;
}

form {
        margin-left: 20%;
        margin-right: 20%;
        width: 60%;
        text-align: center;
        color: #fff;
}

form span {
        float: left;
}

input[type="text"],
textarea {
        width: 550px;
        background-color: #f9f9f9;
        border: 1px solid #eaeaea;
        border-radius: 2px;
        padding: 4px;
        font-size: 18px;
        font-family: "fira-sans", sans-serif;
}

input[name="cf_email"],
textarea {
        margin-top: 12px;
}

textarea {
        height: 150px;
}

input[type="submit"],
input[type="reset"] {
        width: 275px;
        margin-top: 6px;
        border: none;
        padding: 8px;
        box-sizing: border-box;
        color: #fff;
        font-family: "fira-sans", sans-serif;
        font-size: 24px;
        border-radius: 4px;
        transition: all .25s ease-in-out;
        cursor: pointer;
}

input[type="submit"] {
        background-color: #50B1E8;
        margin-right: 4px;
}

input[type="submit"]:hover {
        background-color: #187FB9;
}

input[type="reset"] {
        background-color: #334352;
        margin-left: 4px;
}

input[type="reset"]:hover {
        background-color: #293642;
}

.message {
        color: #fff;
        font-family: "fira-sans", sans-serif;
        font-size: 16px;
        text-align: center;
        margin-bottom: 12px;
}

.message a {
        color: #FFCF0B;
        text-decoration: none;
}

.message.light {
        color: #ababab;
}

.footer {
        text-align: center;
        font-size: 12px;
        font-family: "fira-sans", sans-serif;
        color: #3F5058;
        margin-top: 24px;
        margin-left: 20%;
        margin-right: 20%;
        border-top: 1px solid #3F5058;
        padding-top: 16px;
}

.sponsors ul {
        list-style-type: none;
}

.sponsors ul li {
        display: inline;
        margin-left: 1em;
        margin-right: 1em;
}

.sponsors ul li a {
        text-decoration: none;
}

@media only screen and (max-device-width: 600px) {
        .nav,
        .clients,
        p,
        .footer {
                margin-left: 5%;
                margin-right: 5%;
        }

        .nav a {
                line-height: 2.75em;
        }

        #avatar {
                width: 100%;
        }

        p {
                width: 90%;
        }

        form {
                width: 96%;
                margin-left: 2%;
                margin-right: 2%;
        }

        input[type="text"],
        textarea {
                width: 100%;
        }

        input[type="submit"],
        input[type="reset"] {
                margin-left: 0px;
                margin-right: 0px;
                width: 100%;
        }
}
</style>
<script src="https://use.typekit.net/ihw1ick.js"></script>
<script>try{Typekit.load({ async: true });}catch(e){}</script>
<script src="//code.jquery.com/jquery-1.11.3.min.js"></script>
<script type="text/javascript">
/**
* author Remy Sharp
* url http://remysharp.com/tag/marquee
*/

(function ($) {
    $.fn.marquee = function (klass) {
        var newMarquee = [],
            last = this.length;

        // works out the left or right hand reset position, based on scroll
        // behavior, current direction and new direction
        function getReset(newDir, marqueeRedux, marqueeState) {
            var behavior = marqueeState.behavior, width = marqueeState.width, dir = marqueeState.dir;
            var r = 0;
            if (behavior == 'alternate') {
                r = newDir == 1 ? marqueeRedux[marqueeState.widthAxis] - (width*2) : width;
            } else if (behavior == 'slide') {
                if (newDir == -1) {
                    r = dir == -1 ? marqueeRedux[marqueeState.widthAxis] : width;
                } else {
                    r = dir == -1 ? marqueeRedux[marqueeState.widthAxis] - (width*2) : 0;
                }
            } else {
                r = newDir == -1 ? marqueeRedux[marqueeState.widthAxis] : 0;
            }
            return r;
        }

        // single "thread" animation
        function animateMarquee() {
            var i = newMarquee.length,
                marqueeRedux = null,
                $marqueeRedux = null,
                marqueeState = {},
                newMarqueeList = [],
                hitedge = false;

            while (i--) {
                marqueeRedux = newMarquee[i];
                $marqueeRedux = $(marqueeRedux);
                marqueeState = $marqueeRedux.data('marqueeState');

                if ($marqueeRedux.data('paused') !== true) {
                    // TODO read scrollamount, dir, behavior, loops and last from data
                    marqueeRedux[marqueeState.axis] += (marqueeState.scrollamount * marqueeState.dir);

                    // only true if it's hit the end
                    hitedge = marqueeState.dir == -1 ? marqueeRedux[marqueeState.axis] <= getReset(marqueeState.dir * -1, marqueeRedux, marqueeState) : marqueeRedux[marqueeState.axis] >= getReset(marqueeState.dir * -1, 
marqueeRedux, marqueeState);

                    if ((marqueeState.behavior == 'scroll' && marqueeState.last == marqueeRedux[marqueeState.axis]) || (marqueeState.behavior == 'alternate' && hitedge && marqueeState.last != -1) || (marqueeState.behavior == 
'slide' && hitedge && marqueeState.last != -1)) {
                        if (marqueeState.behavior == 'alternate') {
                            marqueeState.dir *= -1; // flip
                        }
                        marqueeState.last = -1;

                        $marqueeRedux.trigger('stop');

                        marqueeState.loops--;
                        if (marqueeState.loops === 0) {
                            if (marqueeState.behavior != 'slide') {
                                marqueeRedux[marqueeState.axis] = getReset(marqueeState.dir, marqueeRedux, marqueeState);
                            } else {
                                // corrects the position
                                marqueeRedux[marqueeState.axis] = getReset(marqueeState.dir * -1, marqueeRedux, marqueeState);
                            }

                            $marqueeRedux.trigger('end');
                        } else {
                            // keep this marquee going
                            newMarqueeList.push(marqueeRedux);
                            $marqueeRedux.trigger('start');
                            marqueeRedux[marqueeState.axis] = getReset(marqueeState.dir, marqueeRedux, marqueeState);
                        }
                    } else {
                        newMarqueeList.push(marqueeRedux);
                    }
                    marqueeState.last = marqueeRedux[marqueeState.axis];

                    // store updated state only if we ran an animation
                    $marqueeRedux.data('marqueeState', marqueeState);
                } else {
                    // even though it's paused, keep it in the list
                    newMarqueeList.push(marqueeRedux);
                }
            }

            newMarquee = newMarqueeList;

            if (newMarquee.length) {
                setTimeout(animateMarquee, 25);
            }
        }

        // TODO consider whether using .html() in the wrapping process could lead to loosing predefined events...
        this.each(function (i) {
            var $marquee = $(this),
                width = $marquee.attr('width') || $marquee.width(),
                height = $marquee.attr('height') || $marquee.height(),
                $marqueeRedux = $marquee.after('<div ' + (klass ? 'class="' + klass + '" ' : '') + 'style="width: ' + width + 'px; overflow: hidden;"><div style="float: left; white-space: nowrap;">' + $marquee.html() + 
'</div></div>').next(),
                marqueeRedux = $marqueeRedux.get(0),
                hitedge = 0,
                direction = ($marquee.attr('direction') || 'left').toLowerCase(),
                marqueeState = {
                    dir : /down|right/.test(direction) ? -1 : 1,
                    axis : /left|right/.test(direction) ? 'scrollLeft' : 'scrollTop',
                    widthAxis : /left|right/.test(direction) ? 'scrollWidth' : 'scrollHeight',
                    last : -1,
                    loops : $marquee.attr('loop') || -1,
                    scrollamount : $marquee.attr('scrollamount') || this.scrollAmount || 2,
                    behavior : ($marquee.attr('behavior') || 'scroll').toLowerCase(),
                    width : /left|right/.test(direction) ? width : height
                };

            // corrects a bug in Firefox - the default loops for slide is -1
            if ($marquee.attr('loop') == -1 && marqueeState.behavior == 'slide') {
                marqueeState.loops = 1;
            }

            $marquee.remove();

            // add padding
            if (/left|right/.test(direction)) {
                $marqueeRedux.find('> div').css('padding', '0 ' + width + 'px');
            } else {
                $marqueeRedux.find('> div').css('padding', height + 'px 0');
            }

            // events
            $marqueeRedux.bind('stop', function () {
                $marqueeRedux.data('paused', true);
            }).bind('pause', function () {
                $marqueeRedux.data('paused', true);
            }).bind('start', function () {
                $marqueeRedux.data('paused', false);
            }).bind('unpause', function () {
                $marqueeRedux.data('paused', false);
            }).data('marqueeState', marqueeState); // finally: store the state

            // todo - rerender event allowing us to do an ajax hit and redraw the marquee

            newMarquee.push(marqueeRedux);

            marqueeRedux[marqueeState.axis] = getReset(marqueeState.dir, marqueeRedux, marqueeState);
            $marqueeRedux.trigger('start');

            // on the very last marquee, trigger the animation
            if (i+1 == last) {
                animateMarquee();
            }
        });

        return $(newMarquee);
    };
}(jQuery));

$(document).ready(function() {
    $('.clients-cell:nth-of-type(2)').marquee();
});
</script>
</head>
<body>
<div id="logo"></div>
<div class="nav">
        <a href="..">Portfolio</a>
    <a href="../resume/">Resume</a>
    <a class="selected">Information</a>
    <a href="//www.facebook.com/georgebarnickphotography" target="_blank">Facebook</a>
    <a href="//www.instagram.com/georgebarnick" target="_blank">Instagram</a>
</div>
<div class="clients">
    <div class="clients-cell" style="padding-right:.5em;">
        Clients:
    </div>
    <div class="clients-cell" style="width:100%;">
        <a href="//www.americanmuscle.com" target="_blank">AmericanMuscle.com</a>
        <a href="http://www.blownmotorz.com" target="_blank">Blown Motorz, LLC</a>
        <a href="http://www.capitalford.com" target="_blank">Capital Ford of Raleigh NC</a>
        <a href="http://www.dominionraceway.com" target="_blank">Dominon Raceway</a>
        <a href="http://extremecustomcollision.com" target="_blank">Extreme Custom Collision</a>
        <a href="http://glas-werks.com" target="_blank">Glas-Werks, LLC Detailing</a>
        <a href="http://midatlanticst.com" target="_blank">Mid-Atlantic Automotive Show & Trade</a>
        <a href="http://www.tarmacapparel.com" target="_blank">Tarmac Apparel</a>
        <a href="http://www.virginiaexotics.com" target="_blank">Virginia Exotics</a>
    </div>
</div>
<h1>About Me</h1>
<div id="avatar">
        <img src="../images/georgebarnick.jpg" alt="Photo by RACERTV.ORG" />
</div>
<p>
        I am a professional automotive photographer from the Northern Virginia and Washington DC area. I have been doing photography since late 2014 and have continued to make great strides toward producing better results each 
time I pick up the camera. I am currently a student at Northern Virginia Community College. I consider myself passion-driven in all of my work, and enjoy my work as an automotive photographer for both my love for cars and zealous 
approach towards quality and artistic photographic results.
</p>
<h1>Pricing</h1>
<p style="text-align: center;">
        Please use the contact form below to inquire about pricing. Photo and canvas prints are also available for purchase.
</p>
<h1 id="Contact">Contact</h1>
<div class="message">
<?php
if ( $_GET['result'] == "success" ) { ?>
        Thank you for the message. We will contact you shortly.<?php
} else if ( $_GET['result'] == "error" ) { ?>
        Message failed to send. Be sure to fill out each field and make sure your email is entered correctly.<br />If this error persists, please manually send an email to <a 
href="mailto:georgebarnick@gmail.com">georgebarnick@gmail.com</a>.<?php
} ?>
</div>
<form action="contact.php" method="post">
    <input type="text" name="cf_name" placeholder="Your name"<?php if ( $_GET['name'] ) { echo ' value="' . $_GET['name'] . '"'; } ?>><br />
    <input type="text" name="cf_email" placeholder="Your email address"<?php if ( $_GET['email'] ) { echo ' value="' . $_GET['email'] . '"'; } ?>><br />
    <textarea name="cf_message" placeholder="Compose your message"><?php if ( $_GET['message'] ) { echo $_GET['message']; } ?></textarea><br />
    <input type="submit" value="Send">
    <input type="reset" value="Clear"><br />
</form>
<div class="footer">
<div class="sponsors">
Sponsors
<ul>
    <li>
        <a href="http://blownmotorz.com" target="_blank">
                <img src="../images/blownmotorz.png" height="100" alt="Blown Motorz, LLC" />
        </a>
    </li>
        <li>
        <a href="//www.facebook.com/Dirty-Works-Automotive-Detailing-LLC-1491878187807284" target="_blank">
                <img src="../images/dirtyworks.png" height="100" alt="Dirty Works Automotive Detailing" />
        </a>
    </li>
    <li>
        <a href="//www.instagram.com/dmv_mustangs" target="_blank">
                <img src="../images/dmvmustangs.png" height="100" alt="DMV Mustangs" />
        </a>
    </li>
</ul>
</div>
</div>
<div class="footer">
Copyright © 2016 George Barnick Photography, LLC. All rights reserved.
</div>
<!-- Quantcast Tag -->
<script type="text/javascript">
var _qevents = _qevents || [];

(function() {
var elem = document.createElement('script');
elem.src = (document.location.protocol == "https:" ? "https://secure" : "http://edge") + ".quantserve.com/quant.js";
elem.async = true;
elem.type = "text/javascript";
var scpt = document.getElementsByTagName('script')[0];
scpt.parentNode.insertBefore(elem, scpt);
})();

_qevents.push({
qacct:"p-PSLuHGN8ESzUr"
});
</script>

<noscript>
<div style="display:none;">
<img src="//pixel.quantserve.com/pixel/p-PSLuHGN8ESzUr.gif" border="0" height="1" width="1" alt="Quantcast"/>
</div>
</noscript>
<!-- End Quantcast tag -->
</body>
</html>
